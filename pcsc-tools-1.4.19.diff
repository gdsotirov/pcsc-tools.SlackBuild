diff -urNad pcsc-tools-1.4.19-orig//Makefile pcsc-tools-1.4.19/Makefile
--- pcsc-tools-1.4.19-orig//Makefile	2012-06-13 17:53:02.000000000 +0300
+++ pcsc-tools-1.4.19/Makefile	2017-01-29 11:50:37.034817399 +0200
@@ -8,7 +8,7 @@
 DESTDIR ?= /usr/local
 
 VERSION := $(shell pwd | sed s/.*tools-//)
-CFLAGS := -Wall -O2 -g -DVERSION=\"$(VERSION)\" $(PCSC_CFLAGS)
+CFLAGS := -Wall -O2 $(SLKCFLAGS) -DVERSION=\"$(VERSION)\" $(PCSC_CFLAGS)
 LDLIBS := $(PCSC_LDLIBS)
 # On xBSD systems use
 #LDLIBS = -lc_r $(PCSC_LDLIBS)
@@ -33,8 +33,8 @@
 	install -d $(DESTDIR)/share/pcsc
 	install -m 644 smartcard_list.txt $(DESTDIR)/share/pcsc
 
-	install -d $(DESTDIR)/share/man/man1/
-	install -m 644 $(MAN) $(DESTDIR)/share/man/man1/
+	install -d $(DESTDIR)/man/man1/
+	install -m 644 $(MAN) $(DESTDIR)/man/man1/
 
 clean:
 	rm -f pcsc_scan.o $(BIN) $(MAN)
